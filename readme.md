# YOUR_NAME — Personal Portfolio


This repository contains the static website for my portfolio. It is built with plain HTML and CSS and served via GitHub Pages.


## How to publish on GitHub Pages
1. Create a new **public** repository named exactly: `YOUR_USERNAME.github.io` (replace `YOUR_USERNAME`).
2. Clone the repository locally:


```bash
git clone https://github.com/YOUR_USERNAME/YOUR_USERNAME.github.io.git
cd YOUR_USERNAME.github.io